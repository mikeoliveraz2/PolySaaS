# Dose Atomic Service Integration Summary (2025-08-30)

## Overview
- DoseRequestController now dynamically executes atomic services based on the `executescript` field in `Instruction`.
- All atomic services must implement `execute_and_save(request, instruction_row)` or `execute(request, instruction_row)`.
- Results are saved to `CallBackData` and users are notified.

## Atomic Services Verified
- **DifferentialEquationService**: Now implements `execute_and_save`, solves ODEs, saves results.
- **CopilotQueryService**: Now implements `execute_and_save`, processes prompt, saves results.

## Testing Setup
- Example script (`dose/services/example_instructions.py`) creates required `Instruction` rows for POST testing.
- POST requests to `/api/copilot_query/` and `/api/differential_equation/` will trigger respective atomic services.

## Example POST Requests
- **CopilotQueryService**: `{ "prompt": "What is the meaning of life?" }`
- **DifferentialEquationService**: `{ "k": 0.3, "y0": 10, "t0": 0, "tf": 10, "n_points": 100 }`

## Next Steps
- Add more atomic services by implementing the standard interface.
- Use the provided script and POST requests for easy testing and extension.

---
Generated by GitHub Copilot on 2025-08-30.
