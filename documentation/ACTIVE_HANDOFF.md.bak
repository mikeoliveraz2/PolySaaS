# Active Handoff — 2026-09-07 (late morning, Laptop)

This file is the canonical startup and end-of-day handoff. Every agent must read it before editing or answering.

## Session Summary
Stopped chasing the Mattermost login loop. Restored passthrough to the last Mattermost BINGO and narrowed MM→Odoo to a webhook that feeds the **same Slack contact topic**. Slack → Odoo is untouched.

## What Was Completed This Session

### 1. Revert Mattermost passthrough to BINGO `8100b79a` (2026-08-02)
Owner: looping started after the parallel MM producer work; last working MM state was the slug-identity BINGO.

Restored from `8100b79a`:
- `dose/passthrough/handlers/mattermost_handler.py`
- `dose/passthrough/middleware.py` (slug lookup + legacy hostname 302)

Do **not** keep the Aug 8 hostname-as-URL-key change (`d7981e58`) — that broke `/pt/admin/mattermost/`.

### 2. Mattermost webhook = Slack contact topic only
`dose/views/mattermost_events_webhook.py` now:
- Verifies Mattermost outgoing-webhook token
- Parses `New contact: Name, email, Company`
- Calls `publish_slack_contact_event()` — **same mailbox, same `slack.message.contact` instruction, same OdooCreatePartner**

Slack files were not edited (`dose/views/slack_events_webhook.py`, `dose/webhook_events.py`).

Route stays: `/hooks/mattermost/events/`

## Do Not Touch
- Slack → Odoo BINGO `e9f5f20f` (frozen)
- Slack webhook, Slack tests, Slack seed command
- Unrelated dirty files this session did not own: `dose/endpoint_actions/*`, `dose/passthrough/handlers/hubspot_handler.py`

## Current State

### Working / restored
- Slack → Odoo contact flow (BINGO 2026-09-06)
- Mattermost passthrough files back to BINGO 8100b79a (needs Waitress restart + browser verify)
- MM webhook publishes onto Slack’s contact topic (code done, not live-tested)

### Pending
1. Restart Waitress / `runall.ps1`
2. Open `http://localhost:8000/pt/admin/mattermost/` (not a Town Square URL)
3. Confirm MM boots (users/me 200, no login loop)
4. Configure Mattermost Outgoing Webhook → `http://localhost:8000/hooks/mattermost/events/`
5. Set TenantApp `extra_config.mm_team_id` + `mm_webhook_token`
6. Post `New contact: Jane Doe, jane@acme.com, Acme Corp` and confirm Odoo partner via the Slack instruction

### Leftover adapter files (unused by the slim webhook)
These were from the parallel-producer attempt. Safe to ignore; do not wire them back into passthrough:
- `dose/mattermost/` (auth, normalizer, feedback)
- `dose/management/commands/seed_mattermost_orchestration.py`
- `dose/tests/test_mattermost_adapter.py`
- `documentation/MATTERMOST_ADAPTER_BUILD_SPEC.md`

## Blockers / Risks
- Passthrough not re-verified in browser after restore
- Waitress still running old modules until restart
- Old browser `MMAUTHTOKEN` (`ebojaniy...`) can still 401 — use incognito if loop returns after restore

## Current Branch & Commit
- **Branch:** cursor/polysniffer-slack-native-capture
- **Latest pushed commit:** 21711bf9 (or later on remote)
- **Mattermost BINGO restore source:** 8100b79a
- **Slack BINGO (keep intact):** e9f5f20f

## Next Actions
1. Restart services
2. Verify Mattermost passthrough at `/pt/admin/mattermost/`
3. Only then test the outgoing webhook → Slack topic → Odoo

---
*Updated 2026-09-07 ~11:35 (UTC+8) — owner directed revert to last Mattermost BINGO, then webhook-only MM→Odoo.*
