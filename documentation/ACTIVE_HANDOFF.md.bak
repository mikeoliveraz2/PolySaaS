# Active Handoff

This file is the canonical startup and end-of-day handoff for PolySaaS.
Every agent (Copilot, Cursor, Windsurf) must read it before work and update it at EOD.

**Date:** 2026-09-12 (Saturday)  
**Session:** Hostinger VPS live — Dokploy  
**Branch:** main  
**Latest commit:** `f4cee1a9` (Hostinger package `54773689`)

## Live infra (operator confirmed)

| Item | Value |
|------|-------|
| Provider | Hostinger VPS |
| Orchestration | **Dokploy** (not bare Traefik-only bootstrap) |
| VPS IP | `187.53.138.235` |
| Domain | `prod-polysaas.cloud` |
| SSH key (laptop) | `~/.ssh/id_ed25519_hostinger` — **not yet authorized** on VPS (Permission denied for root/ubuntu/deploy) |

## Completed (repo)

- [`deploy/hostinger/`](../deploy/hostinger/) Compose + scripts + [`HOSTINGER_DEPLOY_RUNBOOK.md`](deployment/HOSTINGER_DEPLOY_RUNBOOK.md)
- Original plan hostnames were `*.polysaas.online` — **live domain is now `prod-polysaas.cloud`** (env/compose hostnames need retarget before bring-up)

## Next

1. Add laptop public key to VPS / Dokploy SSH access (or confirm which key Dokploy used)
2. Decide subdomain map under `prod-polysaas.cloud`, e.g. `app` / `mm` / `odoo`
3. Deploy stack via Dokploy (compose project) or SSH + `bringup.sh` — avoid double Traefik if Dokploy already terminates TLS
4. Migrate data + retarget Slack/HubSpot webhooks to production URLs
5. Smoke tests

## Prior bingo still valid

- Slack / Mattermost / HubSpot → Odoo contacts (`newpolysaascontact` + HubSpot FlowLink)
- Latest HubSpot bingo: `11a0d627`

## Parked

- DigitalOcean / AWS
- Apex `polysaas.online` WordPress unchanged
