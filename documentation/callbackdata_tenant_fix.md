# Multi-Tenant CallbackData Fix and Schema Safety

## Summary
- **Issue:** Instructions with `save_callbackdata` enabled were not saving to CallBackData due to schema context issues.
- **Fix:** Middleware and controller logic were updated to always set the PostgreSQL `search_path` to the correct tenant schema before querying or saving any user data.
- **Validation:**
  - Debug output confirmed the correct schema was used for all user requests.
  - `/admin/dose/task/` instruction now writes CallBackData as expected (see admin UI screenshot).
  - Codebase scan confirmed no user-facing queries use the public schema.

## Key Changes
- `dose/doserequestcontroller.py` now sets `search_path` to `<tenant>,public` before all Instruction and CallBackData operations.
- Debug logging added to confirm schema context and instruction matching.
- All user-facing queries are tenant-only; public schema is only used for admin, framework, or migration scripts.

## Results
- CallBackData is now reliably written for tenant instructions.
- No user data is ever queried from the public schema in user-facing code.
- Admin UI confirms new CallBackData records are present for tenant instructions.

## Screenshot
- See attached admin UI screenshot for CallBackData records.

## Codebase Scan
- All public schema usage is limited to:
  - Admin-only operations
  - Django Sites framework (required)
  - Migration/utility scripts
- No violations found in user-facing request processing.

## Next Steps
- Demo is ready.
- If further restrictions or documentation are needed, update as required.

---
*Document generated by GitHub Copilot, January 16, 2026.*
