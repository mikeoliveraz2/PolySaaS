# Git LFS was removed (2026-04): GitHub LFS budget blocked Render clones. Do not commit large
# binaries here — use Drive / release assets / object storage. See documentation/Capital Raise Project/README-decks-off-repo.md

# Shell scripts for Linux containers must stay LF (CRLF breaks shebang on Render/Docker).
deploy/odoo-render/*.sh text eol=lf
