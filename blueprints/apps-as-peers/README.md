# PolySaaS Blueprint: Apps as Peers

## What this is
A minimal Node.js service placeholder representing your internal "Apps as Peers" service.

## Quick start
1. `copy .env.example .env`
2. Edit `.env`
3. `docker compose --env-file .env up -d --build`
4. `./provision.sh`
