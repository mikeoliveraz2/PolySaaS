__pycache__/
*.pyc
.env
.env.key
.env.laptop-backup
.env.*-backup
.env*.backup
.worktree-path
.env.railway.local
.env.wordpress
.env.bak
.env.bak2
# Contains ODOO_MASTER_PASSWORD in plaintext (db-manager admin_passwd) — see odoo.conf.example
odoo.conf
# Local cache: maps ML screenshot filenames → WordPress media URLs (wp_sync_ml_page.py)
.wp-ml-screenshots.json
.DS_Store
Thumbs.db
node_modules/
venv/
.idea/
*.log
*.pem
~$*

# WordPress site — managed by Hostinger, covered by daily backups
dose/website/

# Cursor Render MCP — keys belong in %USERPROFILE%\.cursor\mcp.json only
/mcp.json
/mcp.json.bak

# Local Render env overlay (real secrets) — never commit
scripts/env-master.local.json

# Large binaries — were Git LFS; removed from Git to avoid LFS quota blocking Render/GitHub CI.
documentation/Capital Raise Project/*.pptx
documentation/Capital Raise Project/*.ppt
large_files_backup/**/*.wpress
large_files_backup/**/*.pptx
# Scratch/utility scripts
update_wp_page*.py
update_founders_page.py
founder_form_*.html
founder-beta-circle-clean.html
founder_form_preview.html
