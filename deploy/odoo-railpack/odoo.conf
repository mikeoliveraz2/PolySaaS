[options]
# Store filestore and sessions on the mounted Railway volume.
data_dir = /var/lib/odoo

# Reverse proxy support for Railway ingress.
proxy_mode = True

# Allow iframe embedding for your existing portal flows.
x_frame_options =
